# Database
DATABASE_URL="postgresql://gcms_user:your_secure_password@localhost:5432/gcms"

# JWT - MUST be changed in production!
# Generate strong secrets: openssl rand -base64 64
JWT_ACCESS_SECRET="your-256-bit-access-secret-change-in-production"
JWT_REFRESH_SECRET="your-256-bit-refresh-secret-change-in-production"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# MinIO / S3
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY="your-minio-access-key"
MINIO_SECRET_KEY="your-minio-secret-key"
MINIO_BUCKET_NAME="gcms-uploads"

# Server
PORT=3005
NODE_ENV=development

# CORS (comma-separated list of allowed origins; unset falls back to the localhost dev list)
CORS_ORIGIN="http://localhost:3000,https://your-app.azurecontainerapps.io"

# Security / networking
LOG_LEVEL="info"
