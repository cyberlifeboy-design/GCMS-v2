# GCMS — DEV OVERRIDES ONLY
# Exposes DB and Service ports for local access. NEVER use alone in production.
services:
  mysql:
    ports:
      - "5431:3306"  # Slot from PORT REGISTRY
  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
  backend:
    ports:
      - "3005:3005"
